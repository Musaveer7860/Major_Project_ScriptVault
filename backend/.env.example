# ScriptVault Environment Variables

# MongoDB connection URI (optional, defaults to mongodb://localhost:27017)
# MONGODB_URI=mongodb://localhost:27017

# Gemini API Key for real-time code analysis (optional, falls back to offline analysis if omitted)
GEMINI_API_KEY=
